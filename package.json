{
  "name": "ipfs-dag-jose",
  "version": "0.0.1",
  "description": "Running instance of an IPFS node encoded with Ceramic Networks DAG-JOSE codec for IPLD standard.",
  "main": "./src/index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "ts-node-dev --respawn --transpile-only --cls --rs ./src/index.ts",
    "build": "rm -rf ./dist && tsc",
    "start": "npm run build && node run ./dist/index.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/AlanVegaDecentralize/IPFS-DAG-JOSE.git"
  },
  "keywords": [
    "IPFS",
    "DAG-JOSE",
    "Typescript",
    "Decentralized"
  ],
  "author": "Alan Vega",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/AlanVegaDecentralize/IPFS-DAG-JOSE/issues"
  },
  "homepage": "https://github.com/AlanVegaDecentralize/IPFS-DAG-JOSE#readme",
  "dependencies": {
    "dag-jose": "^0.3.0",
    "dotenv": "^8.2.0",
    "ipfs-core": "^0.3.0",
    "multiformats": "^3.0.3"
  },
  "devDependencies": {
    "@types/node": "^14.14.10",
    "ts-node-dev": "^1.0.0",
    "typescript": "^4.1.2"
  }
}
